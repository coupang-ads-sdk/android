<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.coupang.ads.nativeViewAds">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:networkSecurityConfig="@xml/network_security_config"
            android:theme="@style/Theme.Demo">
        <activity android:name="com.coupang.ads.nativeads.MainActivityKotlin"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="coupang_ads_affiliate_id"
                android:value="AFSDKDEMO"/>

        <meta-data android:name="coupang_ads_sub_id"
                android:value="BannarAdsDemo"/>
    </application>

</manifest>